import { DishesFilterPipe } from './dishes-filter.pipe';

describe('DishesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DishesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
